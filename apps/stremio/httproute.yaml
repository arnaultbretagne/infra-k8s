apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: aiostreams
spec:
  parentRefs:
    - name: bretagne-gateway
      namespace: traefik
      sectionName: https
  hostnames:
    - stremio.bretagne.dev
  rules:
    - backendRefs:
        - name: aiostreams
          port: 3000
