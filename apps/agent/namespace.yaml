apiVersion: v1
kind: Namespace
metadata:
  name: agent
  # Deliberately unlabelled (no platform.bretagne.dev/profile): this pod still mixes trusted
  # control plane with untrusted compute. F3 (ADR 0024) is redefined by infra-k8s ADR 0028:
  # evacuate compute to `agent-runs` (loges now; the shared-substrate pod in a gated follow-up),
  # then relabel this namespace `private-app`. Do NOT label it before then — the ADR 0027 VAP
  # would Deny its mixed-trust pods.
