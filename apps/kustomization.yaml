apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - shared
  - pocket-id
  - stremio
  - code-server
  - obsidian
  # `agent` (the channels-era hub) was decommissioned 2026-08-07 — replaced by `agora`.
  - agora
  - agora-onecli
  - agora-runs
  - terminal
