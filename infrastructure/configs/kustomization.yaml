apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - local-path
  # ⚠️ cilium-lb is Phase 7 ONLY. Referencing it mid-build makes Cilium announce the
  #    single public IP (LB pool + L2/ARP); on this single-NIC VPS the eBPF datapath then
  #    seizes eth0 → SSH lockout (post-mortem 2026-06-28). Keep UNREFERENCED until the flip.
  # - cilium-lb
  - cert-manager-issuers
  - flux-hardening
  - priorityclasses
  - sandboxed-runtime
  - policies
  - preview
