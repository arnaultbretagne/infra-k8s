apiVersion: v1
kind: ResourceQuota
metadata:
  name: preview-quota
  namespace: preview
spec:
  hard:
    requests.cpu: "1"
    limits.cpu: "2"
    requests.memory: 1Gi
    limits.memory: 2Gi
    requests.storage: 10Gi
    persistentvolumeclaims: "2"
    pods: "10"
