apiVersion: v1
kind: Namespace
metadata:
  name: cert-manager
  labels:
    platform.bretagne.dev/profile: infra   # ADR 0024 — platform control-plane component
    pod-security.kubernetes.io/enforce: baseline
